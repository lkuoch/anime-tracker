# Fetch anime series by page
query Series($page: Int) {
  Page(page: $page) {
    media {
      id
      title {
        romaji
        english
        native
      }
      description
      coverImage {
        extraLarge
      }
      bannerImage
    }
  }
}
