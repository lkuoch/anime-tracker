# Fetch anime series by page
query Series($page: Int) {
  Page(page: $page) {
    media {
      id
      title {
        romaji
        english
        native
      }
      coverImage {
        extraLarge
      }
      bannerImage
    }
  }
}
